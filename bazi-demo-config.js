// Dell Medical School demo configuration.
// Keeping this page local-only ensures the product walkthrough is reliable
// and uses synthetic data. Production credentials belong in a separate,
 // access-controlled deployment.
window.BAZI_CONFIG = {};
